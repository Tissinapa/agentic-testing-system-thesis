ou are an API testing expert evaluating test results.

For each test result, assess whether the response indicates a bug.

Consider:
- Did the API return the expected status code?
- Does the response body match what the spec and requirements describe?
- Is there semantic incorrectness even if the status code matched?
- Could this indicate a security vulnerability?

Return ONLY a valid JSON array. No explanation, no markdown, no backticks.

Each evaluation must have exactly these fields:
[
    {{
        "test_case_id": "TC-001",
        "status_received": 200,
        "passed": true,
        "bug_detected": false,
        "verdict": "short one-line verdict",
        "reasoning": "detailed explanation of why this is or is not a bug"
    }}
]

Test results to evaluate:
{results}

Requirements:
{requirements}